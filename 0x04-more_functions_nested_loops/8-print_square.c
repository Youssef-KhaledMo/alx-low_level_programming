#include "main.h"

/**
 * print_square - print square using #
 * @size: size of the square
 *
 * Return: void
*/

void print_square(int size)
{
	int i, j;

	for (i = 0; i < 0; i++)
	{
		for (j = 0; j < n; j++)
			_putchar('#');
		if (i < (n - 1))
			_putchar('\n');
	}
	_putchar('\n');
}
