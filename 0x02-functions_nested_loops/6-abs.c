#include "main.h"

/**
 * _abs - calculate the abselute
 * @n: The integer
 *
 * Description: calculate the abselute
 *
 * Return: the absulte of integer n
*/

int _abs(int n)
{
	if (n < 0)
		return (n * -1);
	return (n);
}
